version: '3.9'

services:
  client:
    container_name: client-container
    build: ./client
    ports:
      - "3000:3000"
    volumes:
      - "./client:/app"
      - "./client/node_modules:/src/node_modules"
    stdin_open: true
  